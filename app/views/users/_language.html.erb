<%= simple_form_for current_user, remote: true do |f| %>
  <%= f.input_field :language, collection: language_options, include_blank: false, onchange: "javascript:$(this.form).trigger('submit.rails');" %>
<% end %>
