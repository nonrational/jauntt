<%= simple_form_for [@room, @message], remote: true do |f| %>
  <%= f.input :text, label: '', placeholder: 'Say something...' %>
  <%= f.button :submit, value: 'Send', 'data-disable-with' => 'Sending...' %>
<% end %>
